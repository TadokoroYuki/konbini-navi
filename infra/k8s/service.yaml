apiVersion: v1
kind: Service
metadata:
  name: konbini-navi-api
  namespace: default
  labels:
    app: konbini-navi-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: konbini-navi-api
